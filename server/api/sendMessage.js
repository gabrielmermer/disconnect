export default defineEventHandler(async (event) => {
  return {
    //this should return a message from the chatgpt api and send it to the ui layer to be displayed
    //this should also save the message to local storage
     
  }
})